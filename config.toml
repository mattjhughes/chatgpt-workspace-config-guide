# Paste these top-level settings into an existing config.toml before its first
# [table] header. Replace the placeholder with the intended Edu workspace UUID.
forced_login_method = "chatgpt"
forced_chatgpt_workspace_id = "REPLACE_WITH_EDU_WORKSPACE_UUID"
